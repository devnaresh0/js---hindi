// Immedialty Invoked Function Expression (IIFE)
(
function chai() {
    // named ifee
    console.log('DB connected');
    
})();

(
    // unnamed ifee
    (name) => {
        console.log(`DD Connedcted ${name}`);
        
    })('hitesh')


// 4:00 Video NO. 24

// chai();

